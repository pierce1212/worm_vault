---
date: 2026-08-17 13:34
related_task: CAN 休眠唤醒
progress: 50%
timeline: "true"
---
# Port_Init → CanSM 到达 FULL_COM 并准备开启 CanIf
![](assets/2026.08.17%20%20奇瑞唤醒时间过久排查/file-20260817140842900.png)


# CanSM 准备开启 CanIf → 首帧 NM Rx
![](assets/2026.08.17%20%20奇瑞唤醒时间过久排查/file-20260817141511444.png)


# `Port_Init → PassiveStartUp`
![](assets/2026.08.17%20%20奇瑞唤醒时间过久排查/file-20260817142207013.png)


# Port_Init → 第一帧 0 x 60 B 发送成功确认
![](assets/2026.08.17%20%20奇瑞唤醒时间过久排查/file-20260817142728142.png)


# Port_Init → CanIf 接受第一帧 NM 发送请求
![](assets/2026.08.17%20%20奇瑞唤醒时间过久排查/file-20260817145144582.png)


## 结论


当前结果：

```
Port → PassiveStartUp         = 371.460 ms
Port → CanIf/XCore接受请求     = 400.366 ms
Port → TxConfirmation         = 462.492 ms
```

拆分为：

```
PassiveStartUp → XCore请求     = 28.906 ms
XCore请求 → TxConfirmation     = 62.126 ms
```

<mark style="background: #FF5582A6;">检查发现 `CanIf_Transmit()` 启用了跨核 Pipe。之前的 400.366 ms 可能只是请求写入 Pipe，并不代表 CAN 驱动已经接收。</mark>

已将 TP 316 终点移动到：

```
Can_Write(...0x60B...)
```

返回 E_OK 之后，位置在：

[CanIf_Transmit.c (line 960)](E:/github/ECAS_qirui_Single_Chamber/BasicSoftware/src/bsw/CanIf/src/CanIf_Transmit.c:960)

当前测试：

```
Port_Init：TP316拉高
目标核Can_Write接受0x60B：TP316拉低
```

本轮 `+脉宽` 记作 `Twrite`：

```
跨核Pipe耗时 = Twrite - 400.366 ms
Can_Write接受 → TxConfirmation = 462.492 ms - Twrite
```

仅针对 `0x60B` 判断，其他 CAN 报文不会触发 TP 316。按要求未编译。

# CanIf 接受 NM 发送请求 → CanNm_TxConfirmation 成功

![](assets/2026.08.17%20%20奇瑞唤醒时间过久排查/file-20260817153144107.png)

#  CanNm_PassiveStartUp有效：拉HIGH  第一帧NM发送请求返回E_OK：拉LOW

![](assets/2026.08.17%20%20奇瑞唤醒时间过久排查/file-20260817160045232.png)




##  结论 

| 阶段                    | 时间         | 占总时间   |
| --------------------- | ---------- | ------ |
| Port_Init → 首帧NM Rx   | 330.000 ms | 约86.4% |
| Rx → PassiveStartUp   | 41.460 ms  | 约10.9% |
| PassiveStartUp → 发送请求 | 10.344 ms  | 约2.7%  |
| 发送请求 → TxConfirmation | 0.260 ms   | 约0.1%  |




# Rx → PassiveStartUp 41.460 ms 优化



结论：逻辑上是正常的异步状态机链路，但 `41.46 ms` 偏长。按当前两个 10 ms 主函数周期推算，理想值通常应在约 `10～20 ms`。

不过要注意：之前的 `41.46 ms` 是用两次冷启动的绝对时间相减得到的：

```
Port → PassiveStartUp  371.46 ms
Port → Rx              330.00 ms
差值                   41.46 ms
```

冷启动存在抖动，因此它还不是可靠的同次直接测量结果。

## 实际执行逻辑

### 1. 收到 NM 报文

CAN 接收中断最终进入：

[CanNm_RxIndication.c (line 355)](E:/github/ECAS_qirui_Single_Chamber/BasicSoftware/src/bsw/CanNm/src/CanNm_RxIndication.c:355)

主要动作是：

```
RamPtr_ps->RxIndication_b = TRUE;
RamPtr_ps->RxStatus_b = TRUE;
```

这里不会直接调用 `CanNm_PassiveStartUp()`，只是设置接收标志。

### 2. 等待 CanNm 10 ms 主函数

BSW 任务周期为 10 ms。`CanNm_MainFunction()` 读取并清除接收标志：

[CanNm.c (line 163)](E:/github/ECAS_qirui_Single_Chamber/BasicSoftware/src/bsw/CanNm/src/CanNm.c:163)

```
PduRxInd_b = RamPtr_ps->RxIndication_b;
RamPtr_ps->RxIndication_b = FALSE;
```

如果 CanNm 处于 Bus Sleep，并且发现收到 NM，就调用：

```
Nm_NetworkStartIndication()
```

对应逻辑在 [CanNm_Inl.h (line 373)](E:/github/ECAS_qirui_Single_Chamber/BasicSoftware/src/bsw/CanNm/api/CanNm_Inl.h:373)。

这一段理论等待：

```
0～10 ms
```

### 3. Nm 通知 ComM

调用链同步执行：

```
Nm_NetworkStartIndication
→ ComM_Nm_NetworkStartIndication
```

ComM 设置被动请求：

[ComM_Nm_NetworkStartIndication.c (line 84)](E:/github/ECAS_qirui_Single_Chamber/BasicSoftware/src/bsw/ComM/src/ComM_Nm_NetworkStartIndication.c:84)

```
PassiveRequestState_u8 |= 0x04;
```

此时只是置标志，也不会立即调用 PassiveStartUp。

### 4. 等待下一次 ComM 10 ms 主函数

当前 10 ms BSW 任务中的调用顺序是：

```
CanSM_MainFunction
→ CAN Mode MainFunction
→ ComM_MainFunction
→ CanNm_MainFunction
```

见 [Rte.c (line 20913)](E:/github/ECAS_qirui_Single_Chamber/BasicSoftware/src/rte/gen/Rte.c:20913)。

关键点是：

```
ComM在CanNm之前运行
```

所以 CanNm 设置 ComM 被动请求时，本周期的 ComM 已经执行完了。这个请求必然等到下一个 10 ms 周期才能处理。

### 5. ComM 处理被动请求

下一次 ComM 主函数读取：

```
PassiveRequest_b =
    (PassiveRequestState_u8 > 0);
```

然后执行状态转换：

```
COMM_NO_COM_NO_PENDING_REQUEST
→ COMM_NO_COM_REQUEST_PENDING
→ 检查CommunicationAllowed_b
→ COMM_FULL_COM_NETWORK_REQUESTED
→ COMM_FULL_COM_READY_SLEEP
→ Nm_PassiveStartUp()
→ CanNm_PassiveStartUp()
```

最终调用位置：

[ComM_LChannelMainFunction.c (line 819)](E:/github/ECAS_qirui_Single_Chamber/BasicSoftware/src/bsw/ComM/src/ComM_LChannelMainFunction.c:819)

## 理论时间

如果 `CommunicationAllowed_b` 已经为 TRUE：

```
Rx → CanNm_MainFunction     = 0～10 ms
等待下一次ComM_MainFunction = 约10 ms
其他同步函数调用             ≈ 很小
--------------------------------------
理论总时间                   ≈ 10～20 ms
```

如果同一次启动直接测得约 41 ms，说明额外等待了约两个 10 ms 周期。重点检查：

```
ComM_ChannelStruct[0].CommunicationAllowed_b
ComM_ChannelStruct[0].ChannelState_e
ComM_ChannelStruct[0].PassiveRequestState_u8
ComM_ChannelStruct[0].BusSmMode_u8
```

最可能是 ComM 收到被动请求后，`CommunicationAllowed_b` 还没有及时变成 TRUE，等待 BswM/CanSM 通信状态准备完成。

## 下一步建议

直接把 TP 316 改成同次测量：

```
CanNm_RxIndication：拉高
CanNm_PassiveStartUp：拉低
```

如果直接结果：

- `10～20 ms`：正常，之前 41.46 ms 主要是跨冷启动相减误差。
- `约40 ms`：确实多等待了两个任务周期，需要继续拆分“Rx→NetworkStartIndication”和“ComM 被动请求→PassiveStartUp”。

<mark style="background: #FF5582A6;">EcuM_Mainfunction 等函数都变成 10 ms</mark>



# Port_Init → 首帧 NM Rx



Port → Transceiver Normal            ≈ 188.75 ms
Transceiver Normal → Controller Start ≈ 64.989 ms
Controller Start → FullCom            ≈ 104.739 ms

