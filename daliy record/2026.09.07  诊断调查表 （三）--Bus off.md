---
date: 2026-09-07 17:21
related_task: 诊断调查表内容开发
progress: 50%
timeline: "true"
---

# bus off


## bus off 疑问

![](assets/2026.09.07%20%20诊断调查表%20（三）--Bus%20off/file-20260907171945199.png)


## 诊断开启为什么需要条件

- **诊断电压满足需求（一般定义诊断电压 9-16 V）**
    
- **网络启动满足一定时间（如网络开启 1 s 以后）**
    
- **特定信号满足需求（如 Power ON 有效）  
    **
- **通信非 BusOff 状态

https://mp.weixin.qq.com/s?__biz=MzUyNDU4NTc1NQ==&mid=2247483718&idx=1&sn=892cd65b90afa81690a144becb08dc13&chksm=fa2a5f32cd5dd6242273879e347699707a665f9a98d37c8b1ba424463eeca1c6e4295e67d8fa&scene=21#wechat_redirect




### 什么是 busoff:

这不是说总线不能用了，而是当前该节点脱离总线，不参与通信，说的更具体一点就是当前节点的 Controller 关闭，节点无法在此期间收/发报文。
注意，此期间 ECU 依然在正常运行，所有的任务依然被 OS 调度。如果节点一直收不到网络管理报文，网络切换到 Bus Sleep Mode，则 ECU 会进入下电流程。


---
### busoff 是如何出现的？
是节点的发送计数器（TEC）数值＞255。
‘NO ACK 错误也会让发送节点的 TEC 不断累加计数，如果总线上就只有一个节点，该节点发送的报文没有节点应答，TEC 是否会累加到 255，进而产生 Busoff 呢？答：不会。’这是一个特殊情况，当总线上只有一个节点时，该节点发送的报文确实没有其他节点可以应答，但是此节点的 TEC 不会累加到 255，而是 128。为什么会这样呢？如果一个总线只有一个节点，也就没有什么通信的必要性了，就一个节点，和谁通信呢？这不就是自言自语吗？既然就一个节点自己玩，那也别脱离总线了，即 Busoff 了，等到其他节点加入再说吧。
![](assets/2026.09.07%20%20诊断调查表%20（三）--Bus%20off/file-20260907171945198.png)![](assets/2026.09.07%20%20诊断调查表%20（三）--Bus%20off/file-20260907171945197.png)


### 如何制造 busoff?
1. 物理短接（- CANH 短接 GND； CANL 短接电源； CANH 与 CANL 短接。）
2. 写测试脚本干扰发送Bit位

### busoff DTC 由哪个模块上报的？
1. Busoff DTC可以由CanSM模块上报给DEM，也可以由SWC，通过RTE上报给DEM。
2. 1）CanSM模块上报
3. 如果是CanSM模块上报Busoff DTC，调用接口Dem_ReportErrorStatus()。按照Autosar规范，CanSM模块属于BSW，因此可以调用该接口，此接口只能由BSW模块调用，而SWC不能使用该接口上报故障状态。
4. （2）SWC模块上报
5. 如果是SWC上报Busoff DTC，则调用Dem_SetEventStatus()接口，实际该接口会被RTE再封装一下（因为走RTE）。
6. 不管是Dem_ReportErrorStatus()还是Dem_SetEventStatus()，两者形参一样，只是为了将SWC和BSW做一个划分。

### 什么是 busoff 的快慢 恢复？
Busoff 产生以后，为保证节点功能的快速恢复，需求中会给出 Busoff 的快/慢恢复时间，即 CanSMBorTimeL 1 与 CanSMBorTimeL 2。
一般来说 CanSMBorTimeL 1 周期会比 CanSMBorTimeL 2 周期小，比如：CanSMBorTimeL 1 = 10 ms，CanSMBorTimeL 2 = 1000 ms。

如果节点在快恢复期间，通信功能没有恢复正常，当快恢复次数达到阈值时就会进入慢恢复期，以此避免功能异常节点过多的干扰总线。Autosar 定义了参数 BusoffCanSMBorCounterL 1 ToL 2 表示快恢复进入慢恢复的次数。

CanSMBorTimeL 1、CanSMBorTimeL 2、BusoffCanSMBorCounterL 1 ToL 2 都是标准的 Autosar 参数，因此，各家 Autosar 供应商的配置工具里面也会提供这 3 个参数的配置，开发者直接按照需求配置即可。
```
注意： 在快/慢恢复期，节点可以接收总线报文。
```

### bus off 上报的使能条件
因为 Busoff 本身就是其他 DTC 事件能否监控的条件之一，这点在开发时注意一下。

### bus off DTC 上报条件
一般来说，需求会明确 Busoff DTC 上报的阈值条件，假设要求快恢复 3 次，Busoff＞5 次，上报对应的 DTC。但是这 5 次在开发时，应该如何计数呢？

### busoff 之后会怎么样呢？
进入 Bus-Off 后：
- ECU 基本停止参与 CAN 通信；
- 不能正常发送应用报文；
- 其他节点收不到本 ECU 的状态；
- 本 ECU 也可能无法获得有效的网络控制指令；
- 控制器需要经过规定的恢复流程才能重新上线。

### 什么是快慢恢复？
Bus-Off 不一定都是永久故障，也可能是瞬时干扰。
因此工程中通常分两级恢复：
- 快速恢复：短时间后快速重新上线，用于偶发干扰；
- 慢速恢复：多次快速恢复失败后延长等待时间，避免故障节点频繁冲击总线。


## bus off 流程


```
CAN 控制器 TEC > 255，ESR1.BOFFINT 置位
→ CAN1_ORED_IRQHandler()
→ FlexCAN_Busoff_Error_IRQHandler(1U)
→ 读取并清除 BOFFINT
→ error_callback(..., FLEXCAN_EVENT_BUSOFF, ...)
→ Can_43_FLEXCAN_ErrorIrqCallback()
→ Can_43_FLEXCAN_ProcessBusOffInterrupt()
→ 停止 CAN Controller
→ CanIf_ControllerBusOff()
→ CanIf切断逻辑发送路径、清理缓存
→ CanIf配置的 User_ControllerBusOff 函数指针
→ CanSM_ControllerBusOff()
→ CanSM启动 Bus-Off 恢复状态机
```

 `CAN_CS_STOPPED` 的目的它表示 **MCAL 管理的物理 CAN Controller 当前已停止**。
 
 Bus-Off 后停止控制器的目的包括：
- 让软件记录状态与硬件状态一致；
- 使控制器不再参与正常发送；
- 给后续恢复建立确定的起点；
- CanSM 恢复时可以按“Stopped→Started”重新启动控制器。

 `CANIF_TX_OFFLINE` 的目的它表示 **CanIf 逻辑发送通道不可用**。硬件已经 Bus-Off，COM、PduR 等上层模块并不知道硬件寄存器的状态。
 
 清空发送缓存的目的：数据时效发生变化
 
 ## 第一次 BUS-OFF

|对象或调用|实际变化|目的|
|---|---|---|
|`CanSM_BusOff_Indicated_ab[]`|置为 `TRUE`|记录该网络出现了待处理的 Bus-Off|
|`BswM_CanSM_CurrentState()`|上报 `CANSM_BSWM_BUS_OFF`|通知模式管理模块网络已进入 Bus-Off|
|`CanSM_BusSMMode_au8[]`|改为 `CANSM_BSM_S_SILENTCOM`|让网络退出正常全通信状态，禁止正常发送|
|`ComM_BusSM_ModeIndication()`|上报 `COMM_SILENT_COMMUNICATION`|通知 ComM 当前只允许静默通信状态|
|`CanSM_currBOR_State_en[]`|单控制器情况下进入 `CANSM_S_RESTART_CC`|准备重启 CAN Controller，开始恢复流程|
|`Dem_SetEventStatus()`|上报 `DEM_EVENT_STATUS_PREFAILED`|告诉 DEM 检测到一次疑似故障，但不是立即确认 DTC|

## ISOLAR 配置

![](assets/2026.09.07%20%20诊断调查表%20（三）--Bus%20off/file-20260907171945195.png)
![](assets/2026.09.07%20%20诊断调查表%20（三）--Bus%20off/file-20260907171945192.png)



CanSM 每次 Bus-Off 上报 `PREFAILED`，DEM 累计到 7 次判失败；成功恢复上报 `PASSED`

![](assets/2026.09.07%20%20诊断调查表%20（三）--Bus%20off/file-20260907171945191.png)

![](assets/2026.09.07%20%20诊断调查表%20（三）--Bus%20off/file-20260907171945183.png)
## 代办事项

电压恢复时间  
当前异常电压恢复仍是 `500 ms`，需求是 `3 s`

阀和电机禁用  
`DFC_stUDSCANBusOff` 目前只有变量定义，没有与 Bus-Off 状态、阀和电机控制连接


## 测试用例


下面这套用例可直接用于 CANoe/CANalyzer＋诊断仪＋可调电源台架。每条用例开始前建议发送 `14 FF FF FF`，然后重新上电。

正常前提统一为：12 V、ECU 唤醒超过 3 秒、DTC Setting 开启（`85 01`）、主 CAN 正常通信。

### Bus-Off 测试用例

|ID|测试目的|操作步骤|预期结果|
|---|---|---|---|
|BO-01|正常状态不误报|正常通信运行 1 分钟，读取 `19 02 FF` |不存在当前或历史 `5E4688`；阀和电机正常可用|
|BO-02|单次瞬时 Bus-Off|制造一次 Bus-Off，在第一次快速恢复时恢复线束|ECU 恢复发送；`5E4688` 不确认；计数复位|
|BO-03|验证 5 次快速恢复|持续保持物理故障，记录每次恢复尝试时间|前 5 次使用 L 1 快速恢复；相邻尝试约 100 ms＋任务/控制器启动延迟|
|BO-04|验证慢速恢复切换|保持故障，观察第 5 次快速恢复以后|后续进入 L 2；第一次慢速恢复等待约 1 s|
|BO-05|验证第 1～6 次不激活 DTC|分别在第 1、2……6 次 Bus-Off 后读取 `19 02 FF` | `5E4688` 未进入 Confirmed/当前激活状态|
|BO-06|验证第 7 次激活 DTC|让 5 次快速恢复全部失败，并让第一次慢速恢复也失败，产生第 7 次 Bus-Off|第 7 次 `PREFAILED` 后 DEM 判定 `FAILED`；`5E4688` 的 TestFailed、Pending、Confirmed 等相关状态位置位|
|BO-07|检查计数边界|在第 6 次 Bus-Off 后恢复一次；重新注入 6 次 Bus-Off|两组 6 次不能累计成 12 次；第二组第 6 次仍不能确认 DTC，第 7 次才确认|
|BO-08|必须成功发送后才算恢复|进入 Bus-Off 后解除物理故障，但临时禁止所有 ECU Tx 报文，使其没有 Tx Confirmation|仅等待 1 s 不能报告 `PASSED`；必须出现成功 Tx Confirmation 后才恢复|
|BO-09|发送仍出错不允许恢复|保持干扰，使控制器重启后继续产生错误帧，不能成功发送|持续执行恢复策略；不得错误上报 `PASSED` |
|BO-10|恢复后转历史 DTC|激活 `5E4688` 后修复线路，确保成功发送 CAN 帧|当前 TestFailed 清除；DTC 记录仍保留为历史/Confirmed，不立即删除|
|BO-11|故障期间禁止阀和电机|激活 Bus-Off DTC，同时发送阀和电机动作请求|阀输出关闭，电机停止或禁止启动；旧控制命令不得在恢复后补执行|
|BO-12|功能恢复|Bus-Off 激活后修复线路并成功发送报文，再发送新的合法控制命令|执行器按确定的恢复策略重新可用；只能执行恢复后的新命令|
|BO-13|10 ms 反应周期|记录第 7 次 Bus-Off、DTC 确认和执行器禁止信号的时间戳|软件反应应在下一次 10 ms 任务内生效；记录实际最大延迟|
|BO-14|主 CAN 与 XCP 隔离|仅对 XCP CAN 制造 Bus-Off，主 CAN 保持正常| `5E4688` 不得出现；当前配置可能出现独立的 XCP DTC `000ABC` |
|BO-15|掉电后诊断记录保存|激活 `5E4688`，等待 NvM 写入，再断电重启|DTC 记录仍能通过 UDS 读取；本轮未完成的 Bus-Off 去抖计数不跨掉电累计|

### 使能条件测试

|ID|测试目的|操作步骤|预期结果|
|---|---|---|---|
|EN-01|唤醒不足 3 秒不诊断|上电后 3 秒内立即制造 7 次 Bus-Off 条件|唤醒条件未满足期间，DEM 计数不累积、DTC 不确认|
|EN-02|唤醒 3 秒边界|分别在 2.99 s 和 3.00 s 后注入故障|2.99 s 时不允许诊断；达到配置有效时间后允许|
|EN-03|低电压抑制|电压设为 8.9 V 并制造 Bus-Off|Bus-Off 恢复机制可运行，但 `5E4688` 不更新或记录|
|EN-04|高电压抑制|电压设为 16.1 V 并制造 Bus-Off|DTC 不更新或记录|
|EN-05|电压边界|分别在 9.0 V、16.0 V 下执行故障测试|两个边界值都属于正常电压范围|
|EN-06|异常电压恢复不足 3 秒|电压先异常，再恢复到 12 V，2.9 秒时制造故障|电压使能条件仍未满足，DTC 不累计|
|EN-07|异常电压恢复满 3 秒|异常电压恢复到 12 V 并稳定 3 秒，再制造故障|允许进行 Bus-Off DTC 判定|
|EN-08|计数中途电压异常|累计 3 次 Bus-Off 后将电压置于范围外，再恢复|因配置为 `DEM_DEBOUNCE_RESET`，旧计数应清除；恢复条件满足后重新从第 1 次累计|
|EN-09| `85 02` 禁止 DTC 设置|发送 `85 02`，随后制造 7 次 Bus-Off|网络恢复策略继续工作，但 DTC 状态和事件存储不得更新|
|EN-10|重新开启 DTC 设置| `85 02` 期间制造故障，之后发送 `85 01` 再注入故障|禁止期间的计数不得带入；开启后重新累计|

### 清除和老化测试

|ID|测试目的|操作步骤|预期结果|
|---|---|---|---|
|CL-01|UDS 主动清除|故障恢复后发送 `14 FF FF FF` | `5E4688` 当前和历史记录被清除|
|CL-02|故障仍在时清除|Bus-Off 物理故障保持期间发送 `14 FF FF FF` |清除服务按配置响应；故障重新检测后 DTC 再次出现|
|CL-03|39 次不应老化删除|恢复故障，连续完成 39 个无故障 POWER OperationCycle|历史 `5E4688` 仍存在|
|CL-04|第 40 次老化删除|完成第 40 个合格无故障 POWER OperationCycle| `5E4688` 按老化策略自动删除|
|CL-05|老化过程被故障打断|老化若干周期后再次激活 Bus-Off|老化计数按 DEM 策略重置或停止，不得错误达到 40 并删除|

### 调试观测点

台架测试时建议同时观察：

```
CanSM_BusOff_Cntr_au8[0]
CanSM_currBOR_State_en[0]
CanSM_BusOff_Indicated_ab[0]
CanSM_TimerConfig_ast[0]
CanIf的Tx Confirmation状态
DTC_0x5E4688_Event的DEM debounce level
DFC_stUDSCANBusOff
ipd_flg_Funinhibit
```

当前预计会直接暴露问题的用例：

- `EN-06/EN-07`：代码目前按 500 ms 恢复电压条件，预计失败。
- `BO-11/BO-12/BO-13`：Bus-Off 尚未连接阀和电机禁用逻辑，预计失败。
- `BO-08`：用于确认 Tx Confirmation Polling 是否真正按预期工作。
- `BO-05/BO-06/BO-07`：用于发现第 6/第 7 次边界是否存在差一错误。